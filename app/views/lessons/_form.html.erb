<%= render 'shared/errors', object: @lesson %>

<%= bootstrap_form_for(@lesson) do |f| %>
  <%= f.text_field :title %>
  <%= f.text_area :content, rows: 15 %>
  <%= f.submit class: 'btn btn-info' %>
<% end %>
